{
    "compilerOptions": {
        "allowJs": true,
        "target": "ES5",
        "outDir": "./built",
        "moduleResolution": "Node"  // Promise를 위해
    },
    "include": ["./src/**/*"]       // 컴파일 시 모든 폴더 (/**) 에 모든 파일 (/*)을 포함 한다.
}